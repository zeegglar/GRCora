# Supabase Configuration (use actual values locally, GitHub secrets in CI)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# For scripts that need direct Supabase access
SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_URL=

# Google Gemini API Key (use actual key locally, GitHub secrets in CI)
VITE_GOOGLE_API_KEY=

# WARNING: NEVER COMMIT SERVICE ROLE KEYS TO GIT!
# Service role keys should only be used in secure server environments
# For seeding, run locally with environment variables only
